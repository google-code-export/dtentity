FIND_PACKAGE(V8 REQUIRED)

SET(TARGET_ADDED_LIBRARIES ${V8_LIBRARIES} dtEntityWrappers)

INCLUDE_DIRECTORIES( 
  ${V8_INCLUDE_DIR}
)

SET(TARGET_SRC pluginfactory.cpp)

SETUP_PLUGIN(dtEntityV8Plugin)
